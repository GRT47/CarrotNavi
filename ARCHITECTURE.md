# TMAP OpenPilot 브릿지 앱 동작 구조 및 아키텍처 명세서

이 문서는 **TMAP 오픈파일럿 브릿지 앱(CarrotNavi)**의 내부 동작 원리와 핵심 구조를 설명합니다.

---

## 1. 앱 개요 (Overview)
이 앱은 안드로이드 기기에서 TMAP 내비게이션 엔진(TMAP SDK)을 구동하여 **현재 주행 정보(제한 속도, 단속 카메라 등)**와 **스마트폰의 GPS 위치 정보**를 추출한 뒤, 이를 **오픈파일럿(OpenPilot)** 기기로 실시간(UDP) 전송하는 역할을 합니다.

---

## 2. 주요 컴포넌트 (Core Components)

앱은 크게 세 가지 핵심 클래스로 구성되어 있습니다.

### 2.1. `MainActivity.kt` (사용자 인터페이스 및 초기화)
- **UI 렌더링:** Jetpack Compose를 사용하여 모던한 UI를 제공합니다. 앱 키(App Key) 입력 화면과 실제 안전운행을 표시하는 지도 화면(Drive Screen)으로 구성됩니다.
- **TMAP SDK 초기화:** 사용자가 입력한 App Key를 바탕으로 `TmapUISDK`를 초기화하고 지도(NavigationFragment)를 화면에 띄웁니다.
- **권한 관리:** 앱이 정상적으로 동작하기 위한 필수 권한(위치, 알림)을 사용자에게 요청합니다.
- **버그 우회 및 생명주기 관리:** TMAP SDK 내부의 네이티브(C/C++) 충돌을 방지하기 위해, 앱 종료 또는 앱 키 변경 시 `Runtime.getRuntime().exit(0)`를 호출하여 프로세스를 깔끔하게 재시작/종료합니다.

### 2.2. `TmapService.kt` (백그라운드 통신 및 데이터 수집)
- **포그라운드 서비스 (Foreground Service):** 앱이 홈 화면으로 내려가더라도(백그라운드) 안드로이드 시스템에 의해 강제 종료되지 않도록 상태창(Notification)에 알림을 띄우고 상시 실행됩니다.
- **GPS 수집 (`LocationManager`):** 1초 단위로 스마트폰의 위경도(Lat/Lon) 및 현재 속도(Speed)를 수집합니다.
- **TMAP 데이터 수집 (`observableEDCData`):** TMAP SDK 내부에서 발생하는 안전운행 이벤트(카메라, 제한 속도 변화 등)를 구독하여 실시간으로 값을 가로챕니다.
- **UDP 패킷 전송:** 수집된 정보를 JSON 포맷으로 변환하여, 로컬네트워크(와이파이, 핫스팟 등)의 브로드캐스트 주소를 통해 오픈파일럿으로 쉴 새 없이(1초 간격 + 이벤트 발생 시) 쏘아 보냅니다.

### 2.3. `TmapDataManager.kt` (상태 관리자)
- **중앙 저장소:** UI(`MainActivity`)와 백그라운드(`TmapService`) 간에 데이터를 주고받기 위한 전역 상태 관리 객체입니다.
- **반응형 상태(StateFlow):** GPS 수신 강도, 현재 속도, 로그 메시지 등을 이 곳에 저장하면 UI가 자동으로 감지하여 화면을 업데이트(Recomposition)합니다.

---

## 3. 핵심 데이터 흐름 (Data Flow)

오픈파일럿으로 데이터가 넘어가는 과정은 다음과 같이 2가지 경로가 합쳐져서 이루어집니다.

```mermaid
graph TD
    A[TMAP Navigation SDK] -- "안전운행 정보 (EDC Data)" --> C(TmapService)
    B[Android GPS 센서] -- "위치 및 속도 (Location)" --> C
    C -- "JSON 병합 및 가공" --> D{네트워크 전송 (UDP 7706)}
    D -- "Localhost (127.0.0.1)" --> E[OpenPilot]
    D -- "Global Broadcast (255.255.255.255)" --> E
    D -- "Subnet Broadcast (ex: 192.168.43.255)" --> E
```

1. **안전운행 정보 (TMAP SDK → TmapService)**
   - 감시 카메라, 과속 방지턱, 톨게이트 등의 정보가 갱신될 때마다 `observableEDCData`를 통해 서비스로 이벤트가 전달됩니다.
2. **위치 정보 (GPS 센서 → TmapService)**
   - 스마트폰의 하드웨어 GPS 센서에서 1초마다 위치와 이동 속도를 측정하여 서비스로 넘깁니다.
3. **병합 및 UDP 전송 (TmapService → OpenPilot)**
   - 두 데이터를 하나의 JSON 객체(`nRoadLimitSpeed`, `vpPosPointLat` 등)로 묶습니다.
   - 와이파이 핫스팟 등 어떠한 연결 상태에서도 오픈파일럿이 데이터를 확실히 받을 수 있도록 **모든 네트워크 인터페이스의 브로드캐스트 주소**를 찾아 동시에 패킷을 발송합니다.

---

## 4. 특화된 메커니즘 (Key Mechanisms)

* **무중단 백그라운드 실행**
  사용자가 다른 앱을 쓰거나 화면을 꺼도, `location` 타입의 포그라운드 서비스가 구동 중이므로 안드로이드의 배터리 최적화 도즈(Doze) 모드를 우회하여 데이터 송신이 끊기지 않습니다.
* **하드코어 브로드캐스팅 로직**
  일부 안드로이드 폰에서 모바일 데이터와 와이파이가 동시 켜져 있을 때 UDP 패킷이 모바일 망으로 새어나가는 현상을 방지하기 위해, 기기에 연결된 모든 서브넷의 브로드캐스트 IP를 역추적하여 전송합니다.
* **음소거 상태 영구 유지**
  오픈파일럿 사용자들은 차량 내비게이션 사운드를 선호하므로, `TmapUISDK.setVolume()`을 활용해 앱 내에서 발생하는 모든 길안내 소리를 원천 차단하는 기능을 내장했습니다. (`SharedPreferences`를 통해 설정값 영구 보존)
* **GitHub 자동 업데이트 (AutoUpdater)**
  개발자가 GitHub에 새로운 APK를 릴리즈하면 앱 실행 시 이를 감지하고 자동으로 다운로드 및 설치 프롬프트를 띄워 사용자가 항상 최신 버전을 유지하게 해줍니다.
